!!!
%html
  = render 'layouts/head'

  %body
    = render 'layouts/topbar'

    .container-fluid
      .sidebar
        .well
          %h5 Sidebar
          %ul
            %li= link_to('Test')
      .content
        .page-header
          %h1= @page_title
        = render :partial => 'layouts/flash', :locals => { :flash => flash }
        = yield
